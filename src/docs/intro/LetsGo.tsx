import React from "react";
import CodeBlock from "../../components/CodeBlock";

export default (
	<React.Fragment>
		Oh, I can't wait! Let's go!
		<br />
		<CodeBlock language="MiM">
			# Using magic variable stdstream!
			<br />
			stdstream = "Hello, world\n";
		</CodeBlock>
	</React.Fragment>
);
