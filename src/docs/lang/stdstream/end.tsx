import React from "react";

export default (
	<React.Fragment>
		<blockquote>
			This property of the <code>stdstream</code> defines the line endings when printing to the console. [default: <code>""</code>]
		</blockquote>
	</React.Fragment>
);
